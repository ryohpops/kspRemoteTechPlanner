/// <reference path="../calculatorreferences.ts" />
module App.Calculator {
    export class Point {
        'use strict';

        x: number;
        y: number;

        constructor(x: number, y: number) {
            this.x = x;
            this.y = y;
        }
    }
}