/// <reference path="../_references.ts" />

module App {
    export interface Body {
        name: string;
        color: string;
        radius: number;
        stdGravity: number;
        soi: number;
    }
}
